<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huynhps09200.duanmau">

    <application
        android:allowBackup="true"
        android:icon="@drawable/avtbook"
        android:label="@string/app_name"
        android:roundIcon="@drawable/avtbook"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ThongkeActivity"></activity>
        <activity android:name=".SalesActivity" />
        <activity android:name=".BillManagerActivity" />
        <activity android:name=".AddTypeActivity" />
        <activity android:name=".AddBookActivity" />
        <activity android:name=".AddPersonalActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".MKActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>